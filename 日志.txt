10.29：路径和权重的选择、交叉、变异操作完成

保存到指定文件夹的分段路径中包含模式信息，坐标第一行为y坐标，第二列为x坐标

分段输出路径开头结尾是起终点

readdata.m读取外部数据的起终点坐标、权重值。

10.30：遗传算法每个指标的计算最后归一化到10-20之间
 
规划平滑度的函数有问题，path_smooth中没有具体的角度信息，只有惩罚信息

查找所有路径也需要改进

10.31：原版a星算法也变成8方向

修改了遗传算法的交叉操作的bug

产生连续路径的操作加入判断：是否超出栅格地图边界

11.1：调整了四个指标计算过程中矩阵的索引，使原地图和GA算法中的地图索引对齐

发现：地图中的障碍物面积占比越大，结果越容易早熟，表现就是selection操作中出现超出索引范围

更改遗传算法最大进化代数为 ln(n)*100 取整

精英策略没有调试完成

11.2：初始种群规模： n*n/2

最大迭代数：ln（n）*100 即 floor(log(n)*100)

权重改为1位小数

权重的选择交叉变异与路径合并

当矩阵中只有一个值时，归一化无效，所以归一化到1到200

权重不收敛

绘制地图时最好每行留出比较多的空格有利于种群产生

11.3：取消权重的迭代

直接产生所有一位小数的权重

11.4：加入精英策略

四个指标：path_cost=log(cost);
                path_risk=log(((risk*100)-(90*ones(1, n)))*10);
                path_smooth=log(pathsmooth);
                path_time=log(time);

11.5：
11.10：将astar删除，title都改为英语
11.11:加入多次执行GA，选出最小值为结果
12.12：加入地图分块，种群多样化算子

2022.3.7：排除地图分析算子中的错误
2022.5.25：删除全局路径规划中的人工势场功能
                 加入dwa局部路径规划
2022.5.26：修改了dwa中障碍物坐标的计算错误	
2022.6.2：全局人工势场回来了;
                删除了冗余代码；
	右侧命令行中显示数据顺序和权重顺序相同
2022.6.6：为全局算法构思了改进方式：在种群扩展算子中排除每次都会被筛选掉的区域，之后不在这些区域中在生成新的个体
	为dwa添加了实时更新障碍物的接口，现在可以在全局地图规划完成后，dwa启动后，手动添加新的障碍物
2022.6.8：发布ros速度指令